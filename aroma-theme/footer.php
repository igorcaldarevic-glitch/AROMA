    <footer class="bg-gray-100 dark:bg-black pt-16 pb-24 border-t border-black/5 dark:border-white/5 relative z-30">
        <div class="absolute inset-0 bg-[#93C572]/40 dark:bg-transparent pointer-events-none"></div>
        <div class="container mx-auto px-6 relative z-10">
            <div class="grid md:grid-cols-3 gap-12 items-center text-center md:text-left">

                <!-- Column 1: Logo & Location -->
                <div class="flex flex-col items-center md:items-start">
                    <div class="w-32 mb-6 relative inline-block">
                        <img src="<?php echo esc_url( get_template_directory_uri() ); ?>/images/webp/AROMA.webp" alt="Aroma"
                            class="w-full h-auto brightness-0 dark:brightness-100 transition-all duration-500">
                    </div>
                    <p
                        class="text-gray-700 dark:text-gray-300 text-sm mb-2 flex items-center gap-2 justify-center md:justify-start">
                        <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5"
                            stroke="currentColor" class="w-4 h-4">
                            <path stroke-linecap="round" stroke-linejoin="round"
                                d="M15 10.5a3 3 0 11-6 0 3 3 0 016 0z" />
                            <path stroke-linecap="round" stroke-linejoin="round"
                                d="M19.5 10.5c0 7.142-7.5 11.25-7.5 11.25S4.5 17.642 4.5 10.5a7.5 7.5 0 1115 0z" />
                        </svg>
                        Slavonski Brod
                    </p>
                    <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5"
                        stroke="currentColor" class="w-4 h-4">
                        <path stroke-linecap="round" stroke-linejoin="round"
                            d="M21.75 6.75v10.5a2.25 2.25 0 01-2.25 2.25h-15a2.25 2.25 0 01-2.25-2.25V6.75m19.5 0A2.25 2.25 0 0019.5 4.5h-15a2.25 2.25 0 00-2.25 2.25m19.5 0v.243a2.25 2.25 0 01-1.07 1.916l-7.5 4.615a2.25 2.25 0 01-2.36 0L3.32 8.91a2.25 2.25 0 01-1.07-1.916V6.75" />
                    </svg>
                    <span class="email-link text-gray-700 dark:text-gray-300 text-sm"
                          data-user="info"
                          data-domain="aroma-since-1923"
                          data-tld="hr">
                        [učitavanje email-a...]
                    </span>
                </div>

                <!-- Column 2: Socials -->
                <div class="flex flex-col items-center">
                    <h4 class="font-serif text-lg mb-6 text-black dark:text-white" data-i18n="footer_follow">Pratite nas
                    </h4>
                    <div class="flex gap-6">
                        <a href="https://www.facebook.com/p/AROMA-SLASTI%C4%8CARNICA-Slavonski-Brod-61568685900060/"
                            target="_blank" rel="noopener noreferrer" class="group"
                            aria-label="Posjetite naš Facebook profil">
                            <div
                                class="p-3 bg-aroma-gold/10 rounded-full text-aroma-gold group-hover:bg-aroma-gold group-hover:text-white transition-colors">
                                <svg class="w-6 h-6" fill="currentColor" viewBox="0 0 24 24" aria-hidden="true">
                                    <path fill-rule="evenodd"
                                        d="M22 12c0-5.523-4.477-10-10-10S2 6.477 2 12c0 4.991 3.657 9.128 8.438 9.878v-6.987h-2.54V12h2.54V9.797c0-2.506 1.492-3.89 3.777-3.89 1.094 0 2.238.195 2.238.195v2.46h-1.26c-1.243 0-1.63.771-1.63 1.562V12h2.773l-.443 2.89h-2.33v6.988C18.343 21.128 22 16.991 22 12z"
                                        clip-rule="evenodd" />
                                </svg>
                            </div>
                        </a>
                        <a href="https://www.instagram.com/aroma.slasticarnica/" target="_blank"
                            rel="noopener noreferrer" class="group" aria-label="Posjetite naš Instagram profil">
                            <div
                                class="p-3 bg-aroma-gold/10 rounded-full text-aroma-gold group-hover:bg-aroma-gold group-hover:text-white transition-colors">
                                <svg class="w-6 h-6" fill="currentColor" viewBox="0 0 24 24" aria-hidden="true">
                                    <path fill-rule="evenodd"
                                        d="M12.315 2c2.43 0 2.784.013 3.808.06 1.064.049 1.791.218 2.427.465a4.902 4.902 0 011.772 1.153 4.902 4.902 0 011.153 1.772c.247.636.416 1.363.465 2.427.048 1.067.06 1.407.06 4.123v.08c0 2.643-.012 2.987-.06 4.043-.049 1.064-.218 1.791-.465 2.427a4.902 4.902 0 01-1.153 1.772 4.902 4.902 0 01-1.772 1.153c-.636.247-1.363.416-2.427.465-1.067.048-1.407.06-4.123.06h-.08c-2.643 0-2.987-.012-4.043-.06-1.064-.049-1.791-.218-2.427-.465a4.902 4.902 0 01-1.772-1.153 4.902 4.902 0 01-1.153-1.772c-.247-.636-.416-1.363-.465-2.427-.047-1.024-.06-1.379-.06-3.808v-.63c0-2.43.013-2.784.06-3.808.049-1.064.218-1.791.465-2.427a4.902 4.902 0 011.153-1.772 4.902 4.902 0 011.772-1.153c.636-.247 1.363-.416 2.427-.465 1.067-.047 1.409-.06 3.809-.06h.63zm1.673 5.378c-.845 0-1.54.695-1.54 1.543 0 .846.695 1.54 1.54 1.54.846 0 1.54-.694 1.54-1.54 0-.848-.695-1.543-1.54-1.543zm-4.69 0c-3.161 0-5.723 2.562-5.723 5.723 0 3.161 2.562 5.723 5.723 5.723 3.16 0 5.722-2.562 5.722-5.723 0-3.16-2.562-5.723-5.722-5.723zm0 2.158c1.968 0 3.565 1.597 3.565 3.565 0 1.968-1.597 3.565-3.565 3.565-1.968 0-3.565-1.597-3.565-3.565 0-1.968 1.597-3.565 3.565-3.565z"
                                        clip-rule="evenodd" />
                                </svg>
                            </div>
                        </a>
                    </div>
                </div>

                <!-- Column 3: Delivery -->
                <div class="flex flex-col items-center md:items-end">
                    <h4 class="font-serif text-lg mb-6 text-black dark:text-white" data-i18n="footer_order">Naručite
                        odmah</h4>
                    <div class="flex flex-col gap-3">
                        <a href="https://wolt.com/hr/hrv/slavonski-brod/restaurant/aroma-slasticarnica" target="_blank"
                            rel="noopener noreferrer"
                            class="text-gray-700 dark:text-gray-300 hover:text-[#00c2e8] transition-colors flex items-center gap-2 justify-center md:justify-end">
                            Wolt
                            <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5"
                                stroke="currentColor" class="w-4 h-4">
                                <path stroke-linecap="round" stroke-linejoin="round"
                                    d="M13.5 6H5.25A2.25 2.25 0 003 8.25v10.5A2.25 2.25 0 005.25 21h10.5A2.25 2.25 0 0018 18.75V10.5m-10.5 6L21 3m0 0h-5.25M21 3v5.25" />
                            </svg>
                        </a>
                        <a href="https://glovoapp.com/hr/hr/slavonski-brod/aroma-slasticarnica-slb/" target="_blank"
                            rel="noopener noreferrer"
                            class="text-gray-700 dark:text-gray-300 hover:text-[#ffc244] transition-colors flex items-center gap-2 justify-center md:justify-end">
                            Glovo
                            <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5"
                                stroke="currentColor" class="w-4 h-4">
                                <path stroke-linecap="round" stroke-linejoin="round"
                                    d="M13.5 6H5.25A2.25 2.25 0 003 8.25v10.5A2.25 2.25 0 005.25 21h10.5A2.25 2.25 0 0018 18.75V10.5m-10.5 6L21 3m0 0h-5.25M21 3v5.25" />
                            </svg>
                        </a>
                    </div>
                </div>
            </div>

            <div
                class="border-t border-black/10 dark:border-white/10 mt-12 pt-8 text-center flex flex-col md:flex-row justify-between items-center gap-4">
                <div class="flex flex-col md:flex-row items-center gap-2 md:gap-4">
                    <p class="text-gray-800 dark:text-gray-200 text-sm font-medium tracking-wider"
                        data-i18n="footer_rights">&copy; <?php echo date('Y'); ?> Slastičarna Aroma. Tradicija od 1923.</p>
                    <a href="<?php echo esc_url( home_url('/politika-privatnosti') ); ?>"
                        class="text-gray-800 dark:text-gray-200 text-sm font-medium tracking-wider hover:text-aroma-gold transition-colors underline underline-offset-2">Politika
                        Privatnosti</a>
                </div>
                <a href="https://icstudio.neocities.org" target="_blank" rel="noopener noreferrer"
                    class="text-gray-800 dark:text-gray-200 text-sm font-medium tracking-wider hover:text-aroma-gold transition-all duration-300 flex items-center gap-2 group opacity-50 hover:opacity-100">
                    <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5"
                        stroke="currentColor" class="w-5 h-5 transition-transform group-hover:scale-110">
                        <path stroke-linecap="round" stroke-linejoin="round"
                            d="M17.25 6.75L22.5 12l-5.25 5.25m-10.5 0L1.5 12l5.25-5.25m7.5-3l-4.5 16.5" />
                    </svg>
                    <span>Web Design: <span style="text-shadow: 0 2px 8px rgba(212, 175, 55, 0.3);">IC
                            Studio</span></span>
                </a>
            </div>
        </div>
    </footer>

    <!-- Back to Top -->
    <div class="fixed bottom-8 right-6 md:right-8 flex flex-col gap-4 z-50">
        <button id="back-to-top"
            class="w-12 h-12 rounded-full bg-aroma-gold text-black shadow-lg flex items-center justify-center hover:scale-110 transition-transform duration-300 opacity-0 invisible translate-y-10"
            aria-label="Idi na početak stranice">
            <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="2"
                stroke="currentColor" class="w-6 h-6">
                <path stroke-linecap="round" stroke-linejoin="round" d="M4.5 15.75l7.5-7.5 7.5 7.5" />
            </svg>
        </button>
    </div>

    <!-- GDPR-Compliant Cookie Consent Banner -->
    <div id="cookie-banner"
        class="fixed bottom-8 left-1/2 -translate-x-1/2 z-[20000] opacity-0 pointer-events-none transition-opacity duration-300">

        <div class="relative bg-gradient-to-br from-amber-100 via-yellow-50 to-amber-50 dark:from-amber-900/95 dark:via-yellow-900/95 dark:to-orange-900/95 backdrop-blur-xl border-4 border-amber-200/60 dark:border-amber-700/60 shadow-2xl rounded-3xl max-w-lg mx-4 overflow-hidden">
            <div class="relative z-10 px-8 py-8">
                <div class="text-center mb-6">
                    <div class="text-6xl mb-3">🍪</div>
                    <h3 class="font-serif text-3xl font-black mb-2 text-amber-950 dark:text-amber-50">
                        Kolačići & Privatnost
                    </h3>
                    <p class="text-sm text-amber-900 dark:text-amber-100 leading-relaxed">
                        Koristimo kolačiće za funkcionalnost stranice i analitiku.
                        <a href="<?php echo esc_url( home_url('/politika-privatnosti') ); ?>" target="_blank"
                           class="text-amber-950 dark:text-amber-50 underline decoration-2 font-bold hover:decoration-amber-900">
                            Politika privatnosti
                        </a>
                    </p>
                </div>

                <!-- Granular Consent Options -->
                <div class="space-y-3 mb-6 text-left text-sm">
                    <label class="flex items-start gap-3 p-3 bg-white/40 dark:bg-black/20 rounded-lg cursor-not-allowed">
                        <input type="checkbox" checked disabled class="mt-1 w-5 h-5 rounded">
                        <div class="flex-1">
                            <div class="font-bold text-amber-950 dark:text-amber-50">Neophodni kolačići</div>
                            <div class="text-xs text-amber-800 dark:text-amber-200 mt-1">
                                Omogućavaju osnovne funkcije stranice (obavezno)
                            </div>
                        </div>
                    </label>

                    <label class="flex items-start gap-3 p-3 bg-white/40 dark:bg-black/20 rounded-lg hover:bg-white/60 dark:hover:bg-black/30 transition-all cursor-pointer">
                        <input type="checkbox" id="analytics-consent" class="mt-1 w-5 h-5 rounded border-amber-700">
                        <div class="flex-1">
                            <div class="font-bold text-amber-950 dark:text-amber-50">Analitički kolačići</div>
                            <div class="text-xs text-amber-800 dark:text-amber-200 mt-1">
                                Google Analytics - pomoću poboljšavamo stranicu
                            </div>
                        </div>
                    </label>

                    <label class="flex items-start gap-3 p-3 bg-white/40 dark:bg-black/20 rounded-lg hover:bg-white/60 dark:hover:bg-black/30 transition-all cursor-pointer">
                        <input type="checkbox" id="marketing-consent" class="mt-1 w-5 h-5 rounded border-amber-700">
                        <div class="flex-1">
                            <div class="font-bold text-amber-950 dark:text-amber-50">Marketing kolačići</div>
                            <div class="text-xs text-amber-800 dark:text-amber-200 mt-1">
                                Personalizirani oglasi i remarketing
                            </div>
                        </div>
                    </label>
                </div>

                <div class="flex flex-col sm:flex-row gap-3">
                    <button id="cookie-decline"
                        class="flex-1 px-6 py-3 rounded-full border-3 border-amber-900/40 dark:border-amber-100/40 bg-amber-200/60 dark:bg-amber-800/60 text-amber-950 dark:text-amber-50 font-black text-sm hover:bg-amber-300/80 dark:hover:bg-amber-700/80 hover:scale-105 transition-all duration-200 shadow-lg">
                        Samo neophodni
                    </button>
                    <button id="cookie-accept-selected"
                        class="flex-1 px-6 py-3 rounded-full bg-gradient-to-r from-yellow-600 via-amber-600 to-yellow-600 text-white font-black text-sm shadow-xl hover:shadow-2xl transition-all duration-300 hover:scale-105">
                        Odabrano
                    </button>
                    <button id="cookie-accept-all"
                        class="flex-1 px-6 py-3 rounded-full bg-gradient-to-r from-aroma-gold via-yellow-500 to-aroma-gold text-amber-950 font-black text-sm shadow-xl shadow-aroma-gold/50 hover:shadow-2xl hover:shadow-aroma-gold/70 transition-all duration-300 hover:scale-110 active:scale-95 border-3 border-yellow-600/50">
                        ✓ Sve
                    </button>
                </div>
            </div>
        </div>
    </div>

    <script>
        const themeToggleBtn = document.getElementById('theme-toggle');
        const mobileThemeToggleBtn = document.getElementById('mobile-theme-toggle');
        const htmlElement = document.documentElement;

        (localStorage.theme === 'dark' || (!('theme' in localStorage) && window.matchMedia('(prefers-color-scheme: dark)').matches))
            ? htmlElement.classList.add('dark') : htmlElement.classList.remove('dark');

        function toggleTheme() {
            htmlElement.classList.toggle('dark');
            localStorage.theme = htmlElement.classList.contains('dark') ? 'dark' : 'light';
        }

        if (themeToggleBtn) themeToggleBtn.addEventListener('click', toggleTheme);
        if (mobileThemeToggleBtn) mobileThemeToggleBtn.addEventListener('click', toggleTheme);

        const backToTopBtn = document.getElementById('back-to-top');
        window.addEventListener('scroll', () => {
            const show = window.scrollY > 500;
            backToTopBtn.classList.toggle('opacity-0', !show);
            backToTopBtn.classList.toggle('invisible', !show);
            backToTopBtn.classList.toggle('translate-y-10', !show);
        });
        backToTopBtn.addEventListener('click', () => window.scrollTo({ top: 0, behavior: 'smooth' }));
    </script>

    <script>
        // GDPR-Compliant Cookie Consent Script
        document.addEventListener('DOMContentLoaded', function () {
            const cookieBanner = document.getElementById('cookie-banner');
            const cookieAcceptAllBtn = document.getElementById('cookie-accept-all');
            const cookieAcceptSelectedBtn = document.getElementById('cookie-accept-selected');
            const cookieDeclineBtn = document.getElementById('cookie-decline');

            if (!cookieBanner) return;

            const cookieConsent = {
                save: (choices) => {
                    try {
                        localStorage.setItem('cookieConsent', JSON.stringify({
                            ...choices,
                            timestamp: Date.now(),
                            version: '1.0'
                        }));
                    } catch (e) {
                        console.error('Failed to save consent:', e);
                    }
                },
                load: () => {
                    try {
                        return JSON.parse(localStorage.getItem('cookieConsent') || '{}');
                    } catch {
                        return {};
                    }
                }
            };

            const existingConsent = cookieConsent.load();
            if (!existingConsent.timestamp) {
                setTimeout(() => {
                    cookieBanner.classList.remove('opacity-0', 'pointer-events-none');
                    cookieBanner.classList.add('show');
                }, 1000);
            }

            if (cookieAcceptAllBtn) {
                cookieAcceptAllBtn.addEventListener('click', () => {
                    cookieConsent.save({ necessary: true, analytics: true, marketing: true });
                    if (typeof loadAnalytics === 'function') loadAnalytics();
                    if (typeof loadMarketing === 'function') loadMarketing();
                    cookieBanner.classList.add('opacity-0', 'pointer-events-none');
                    cookieBanner.classList.remove('show');
                });
            }

            if (cookieAcceptSelectedBtn) {
                cookieAcceptSelectedBtn.addEventListener('click', () => {
                    const analytics = document.getElementById('analytics-consent')?.checked || false;
                    const marketing = document.getElementById('marketing-consent')?.checked || false;
                    cookieConsent.save({ necessary: true, analytics: analytics, marketing: marketing });
                    if (analytics && typeof loadAnalytics === 'function') loadAnalytics();
                    if (marketing && typeof loadMarketing === 'function') loadMarketing();
                    cookieBanner.classList.add('opacity-0', 'pointer-events-none');
                    cookieBanner.classList.remove('show');
                });
            }

            if (cookieDeclineBtn) {
                cookieDeclineBtn.addEventListener('click', () => {
                    cookieConsent.save({ necessary: true, analytics: false, marketing: false });
                    cookieBanner.classList.add('opacity-0', 'pointer-events-none');
                    cookieBanner.classList.remove('show');
                });
            }
        });

        function loadAnalytics() {
            const consent = JSON.parse(localStorage.getItem('cookieConsent') || '{}');
            if (!consent.analytics) return;
            // Uncomment and add your GA4 Measurement ID:
            /*
            (function(w,d,s,l,i){w[l]=w[l]||[];w[l].push({'gtm.start':
            new Date().getTime(),event:'gtm.js'});var f=d.getElementsByTagName(s)[0],
            j=d.createElement(s),dl=l!='dataLayer'?'&l='+l:'';j.async=true;j.src=
            'https://www.googletagmanager.com/gtag/js?id='+i;f.parentNode.insertBefore(j,f);
            })(window,document,'script','dataLayer','G-XXXXXXXXXX');
            window.dataLayer = window.dataLayer || [];
            function gtag(){dataLayer.push(arguments);}
            gtag('js', new Date());
            gtag('config', 'G-XXXXXXXXXX', { anonymize_ip: true, cookie_flags: 'SameSite=None;Secure' });
            */
        }

        window.addEventListener('load', loadAnalytics);
    </script>

    <script>
        const orderModal = document.getElementById('cake-order-modal');
        const orderBackdrop = document.getElementById('order-backdrop');
        const orderContent = document.getElementById('order-modal-content');
        const orderClose = document.getElementById('order-close');
        const orderBtn = document.getElementById('order-cake-btn');
        const orderForm = document.getElementById('cake-order-form');
        const thankYouModal = document.getElementById('thank-you-modal');
        const thankYouContent = document.getElementById('thank-you-content');
        const thankYouClose = document.getElementById('thank-you-close');

        const SUBMIT_COOLDOWN = 60000;
        const MIN_FILL_TIME = 3000;
        let lastSubmitTime = 0;
        let formLoadTime = Date.now();

        function openOrderModal() {
            orderModal.classList.remove('opacity-0', 'pointer-events-none');
            orderContent.classList.remove('scale-95');
            orderContent.classList.add('scale-100');
            document.body.style.overflow = 'hidden';
            formLoadTime = Date.now();
        }

        function closeOrderModal() {
            orderModal.classList.add('opacity-0', 'pointer-events-none');
            orderContent.classList.remove('scale-100');
            orderContent.classList.add('scale-95');
            document.body.style.overflow = '';
        }

        function showThankYou() {
            closeOrderModal();
            setTimeout(() => {
                thankYouModal.classList.remove('opacity-0', 'pointer-events-none');
                thankYouContent.classList.remove('scale-95');
                thankYouContent.classList.add('scale-100');
            }, 300);
        }

        function closeThankYou() {
            thankYouModal.classList.add('opacity-0', 'pointer-events-none');
            thankYouContent.classList.remove('scale-100');
            thankYouContent.classList.add('scale-95');
            document.body.style.overflow = '';
        }

        if (orderBtn) orderBtn.addEventListener('click', openOrderModal);
        if (orderClose) orderClose.addEventListener('click', closeOrderModal);
        if (orderBackdrop) orderBackdrop.addEventListener('click', closeOrderModal);
        if (thankYouClose) thankYouClose.addEventListener('click', closeThankYou);

        if (orderForm) {
            orderForm.addEventListener('submit', async (e) => {
                e.preventDefault();
                const now = Date.now();

                if (now - lastSubmitTime < SUBMIT_COOLDOWN) {
                    alert('⏱️ Molimo pričekajte 1 minutu prije ponovnog slanja.');
                    return;
                }

                if (now - formLoadTime < MIN_FILL_TIME) {
                    console.warn('⚠️ Submission too fast - possible bot');
                    return;
                }

                const honeypot1 = document.getElementById('website')?.value || '';
                const honeypot2 = document.getElementById('website_extra')?.value || '';
                const honeypot3 = orderForm.querySelector('[name="phone_backup"]')?.value || '';

                if (honeypot1 !== "" || honeypot2 !== "" || honeypot3 !== "") {
                    console.warn('🤖 Bot detected via honeypot');
                    return;
                }

                orderForm.classList.add('was-validated');
                if (!orderForm.checkValidity()) {
                    orderForm.reportValidity();
                    return;
                }

                const submitBtn = orderForm.querySelector('button[type="submit"]');
                const originalBtnText = submitBtn.textContent;
                submitBtn.disabled = true;
                submitBtn.textContent = 'Šaljem...';
                submitBtn.classList.add('opacity-50', 'cursor-not-allowed');
                const formData = new FormData(orderForm);

                try {
                    const response = await fetch(orderForm.action, {
                        method: 'POST',
                        body: formData,
                        headers: { 'Accept': 'application/json' }
                    });

                    if (response.ok) {
                        showThankYou();
                        orderForm.reset();
                        lastSubmitTime = now;
                    } else {
                        const data = await response.json();
                        alert('⚠️ Došlo je do greške. Molimo pokušajte ponovo.');
                        console.error('Form error:', data);
                    }
                } catch (error) {
                    alert('⚠️ Greška pri slanju. Provjerite internet konekciju.');
                    console.error('Network error:', error);
                } finally {
                    submitBtn.disabled = false;
                    submitBtn.textContent = originalBtnText;
                    submitBtn.classList.remove('opacity-50', 'cursor-not-allowed');
                }
            });

            const phoneInput = orderForm.querySelector('input[name="phone"]');
            if (phoneInput) {
                phoneInput.addEventListener('blur', function () {
                    const phone = this.value.trim();
                    this.setCustomValidity(phone && !/^(\+385|0)[0-9]{8,9}$/.test(phone.replace(/\s/g, ''))
                        ? 'Unesite ispravan hrvatski broj telefona (npr. 0912345678 ili +385912345678)' : '');
                    if (this.validationMessage) this.reportValidity();
                });
            }
            const emailInput = orderForm.querySelector('input[name="email"]');
            if (emailInput) {
                emailInput.addEventListener('blur', function () {
                    const email = this.value.trim();
                    this.setCustomValidity(email && !/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email)
                        ? 'Unesite ispravnu email adresu' : '');
                    if (this.validationMessage) this.reportValidity();
                });
            }
        }
        document.addEventListener('keydown', (e) => {
            if (e.key === 'Escape') { closeOrderModal(); closeThankYou(); }
        });
    </script>

    <script>
        const heroVideo = document.getElementById('hero-video');
        let isVideoActive = false;
        function activateVideo() {
            if (!isVideoActive && heroVideo) { heroVideo.play().catch(() => {}); isVideoActive = true; }
        }
        document.addEventListener('mousemove', activateVideo, { once: true });
        window.addEventListener('scroll', activateVideo, { once: true });
        document.addEventListener('touchstart', activateVideo, { once: true });
    </script>

    <script>
        const modal = document.getElementById('cake-modal');
        const modalBackdrop = document.getElementById('modal-backdrop');
        const modalContent = document.getElementById('modal-content');
        const modalClose = document.getElementById('modal-close');
        const modalImg = document.getElementById('modal-img');
        const modalTitle = document.getElementById('modal-title');
        const modalDesc = document.getElementById('modal-desc');
        const triggers = document.querySelectorAll('.cake-hover-trigger');

        function openModal(title, desc, imgSrc) {
            modalTitle.textContent = title;
            modalDesc.textContent = desc;
            modalImg.src = imgSrc;
            modal.classList.remove('opacity-0', 'pointer-events-none');
            modalContent.classList.remove('scale-95');
            modalContent.classList.add('scale-100');
            document.body.style.overflow = 'hidden';
        }

        function closeModal() {
            modal.classList.add('opacity-0', 'pointer-events-none');
            modalContent.classList.remove('scale-100');
            modalContent.classList.add('scale-95');
            document.body.style.overflow = '';
        }

        triggers.forEach(trigger => {
            trigger.addEventListener('click', () => {
                const title = trigger.getAttribute('data-title');
                const desc = trigger.getAttribute('data-desc');
                const img = trigger.querySelector('img').src;
                openModal(title, desc, img);
            });
        });

        if (modalClose) modalClose.addEventListener('click', closeModal);
        if (modalBackdrop) modalBackdrop.addEventListener('click', closeModal);
        document.addEventListener('keydown', (e) => { if (e.key === 'Escape') closeModal(); });

        document.querySelectorAll('#cakes-scroll-container .cake-hover-trigger[data-title]').forEach(item => {
            const title = item.getAttribute('data-title');
            if (title) {
                const caption = document.createElement('div');
                caption.textContent = title;
                caption.className = 'text-center text-sm font-semibold text-gray-800 dark:text-white py-2 px-3 bg-white/90 dark:bg-black/80';
                item.appendChild(caption);
            }
        });

        const scrollContainer = document.getElementById('cakes-scroll-container');
        const scrollLeftBtn = document.getElementById('scroll-left');
        const scrollRightBtn = document.getElementById('scroll-right');

        if (scrollContainer && scrollLeftBtn && scrollRightBtn) {
            scrollLeftBtn.addEventListener('click', () => scrollContainer.scrollBy({ left: -400, behavior: 'smooth' }));
            scrollRightBtn.addEventListener('click', () => scrollContainer.scrollBy({ left: 400, behavior: 'smooth' }));
        }
    </script>

    <script>
        gsap.registerPlugin(ScrollTrigger);

        window.addEventListener('DOMContentLoaded', () => {
            const kineticWords = document.querySelectorAll('.kinetic-word');
            if (kineticWords.length > 0) {
                gsap.set(kineticWords, { opacity: 0, y: 60, rotationX: -90, scale: 0.5, transformPerspective: 1000 });
                kineticWords.forEach((word, i) => {
                    gsap.to(word, { opacity: 1, y: 0, rotationX: 0, scale: 1, duration: 1.2, delay: 0.5 + (i * 0.15), ease: "back.out(1.7)", clearProps: "transform" });
                });
            }

            window.addEventListener('languageChanged', () => {
                setTimeout(() => {
                    const newWords = document.querySelectorAll('.kinetic-word');
                    gsap.set(newWords, { opacity: 0, y: 60, rotationX: -90, scale: 0.5, transformPerspective: 1000 });
                    newWords.forEach((word, i) => {
                        gsap.to(word, { opacity: 1, y: 0, rotationX: 0, scale: 1, duration: 1.2, delay: i * 0.15, ease: "back.out(1.7)", clearProps: "transform" });
                    });
                }, 50);
            });
        });

        const revealText = document.querySelector(".reveal-text");
        const revealImage = document.querySelector(".reveal-image");

        if (revealText || revealImage) {
            const tl = gsap.timeline();
            if (revealText) tl.from(".reveal-text", { x: -50, opacity: 0, duration: 1.5, ease: "power3.out" });
            if (revealImage) tl.from(".reveal-image", { x: 50, opacity: 0, duration: 1.5, ease: "power3.out" }, "-=1.2");
        }

        const floatingElements = gsap.utils.toArray(".floating-element:not(.constrained-image)");
        if (floatingElements.length > 0) {
            floatingElements.forEach((el, i) => {
                const speed = parseFloat(el.getAttribute('data-speed')) || 0.5;
                const rect = el.getBoundingClientRect();
                const isLeft = rect.left < window.innerWidth / 2;
                const xDir = isLeft ? -1 : 1;
                const maxScroll = document.documentElement.scrollHeight - window.innerHeight;
                const movementY = maxScroll * (0.8 + speed * 0.4);
                const movementX = xDir * 50 * speed;
                gsap.to(el, { y: movementY, x: movementX, rotation: i % 2 === 0 ? 180 : -180, ease: "none",
                    scrollTrigger: { trigger: "body", start: "top top", end: "bottom bottom", scrub: 0.5 } });
            });
        }

        const aboutTextReveal = document.querySelector('.about-text-reveal');
        if (aboutTextReveal) {
            gsap.from(aboutTextReveal, { x: 200, opacity: 0, duration: 3, ease: "power1.out",
                scrollTrigger: { trigger: "#o-nama", start: "top 85%", end: "top 15%", scrub: 2, toggleActions: "play none none reverse" } });
        }

        const fadeUpElements = gsap.utils.toArray('.scroll-fade-up');
        fadeUpElements.forEach(el => {
            gsap.from(el, { y: 60, opacity: 0, duration: 1, ease: "power2.out",
                scrollTrigger: { trigger: el, start: "top 85%", end: "top 60%", toggleActions: "play none none reverse" } });
        });

        const imageLeftElements = gsap.utils.toArray('.scroll-image-left');
        imageLeftElements.forEach(el => {
            gsap.from(el, { x: -100, opacity: 0, duration: 1.2, ease: "power3.out",
                scrollTrigger: { trigger: el, start: "top 80%", toggleActions: "play none none reverse" } });
        });

        const imageRightElements = gsap.utils.toArray('.scroll-image-right');
        imageRightElements.forEach(el => {
            gsap.from(el, { x: 100, opacity: 0, duration: 1.2, ease: "power3.out",
                scrollTrigger: { trigger: el, start: "top 80%", toggleActions: "play none none reverse" } });
        });

        const scaleUpElements = gsap.utils.toArray('.scroll-scale-up');
        scaleUpElements.forEach((el, i) => {
            gsap.from(el, { scale: 0.8, opacity: 0, duration: 0.8, delay: i * 0.1, ease: "back.out(1.7)",
                scrollTrigger: { trigger: el, start: "top 85%", toggleActions: "play none none reverse" } });
        });

        const staggerContainers = document.querySelectorAll('#ponuda .grid');
        staggerContainers.forEach(container => {
            const items = container.querySelectorAll('.scroll-stagger-item');
            if (items.length > 0) {
                gsap.from(items, { y: 80, opacity: 0, duration: 0.8, stagger: 0.15, ease: "power2.out",
                    scrollTrigger: { trigger: container, start: "top 75%", toggleActions: "play none none reverse" } });
            }
        });

        const pancakesText = document.querySelector('.pancakes-text-reveal');
        if (pancakesText) {
            gsap.from(pancakesText, { x: -120, opacity: 0, duration: 1.5, ease: "power2.out",
                scrollTrigger: { trigger: "#palacinke", start: "top 70%", toggleActions: "play none none reverse" } });
        }

        const pancakesImage = document.querySelector('.pancakes-image-reveal');
        if (pancakesImage) {
            gsap.from(pancakesImage, { x: 120, opacity: 0, rotation: 10, duration: 1.5, ease: "power2.out",
                scrollTrigger: { trigger: "#palacinke", start: "top 70%", toggleActions: "play none none reverse" } });
        }

        const pancakeItems = gsap.utils.toArray('.pancake-item');
        if (pancakeItems.length > 0) {
            gsap.from(pancakeItems, { x: -50, opacity: 0, duration: 0.6, stagger: 0.2, ease: "back.out(1.7)",
                scrollTrigger: { trigger: ".pancakes-list", start: "top 80%", toggleActions: "play none none reverse" } });
        }

        const constrainedImages = gsap.utils.toArray(".constrained-image");
        if (constrainedImages.length > 0) {
            constrainedImages.forEach((el, i) => {
                gsap.set(el, { autoAlpha: 0 });
                ScrollTrigger.create({
                    trigger: "#ponuda", start: "top bottom", endTrigger: "#dostava", end: "center top",
                    onToggle: self => { gsap.to(el, { autoAlpha: self.isActive ? 1 : 0, duration: 0.5, overwrite: true }); }
                });
                const speed = parseFloat(el.getAttribute('data-speed')) || 0.5;
                gsap.to(el, { y: 100 * speed, rotation: i % 2 === 0 ? 15 : -15, ease: "none",
                    scrollTrigger: { trigger: "#ponuda", start: "top bottom", endTrigger: "#dostava", end: "bottom top", scrub: 1 } });
            });
        }

        const revealUpElements = gsap.utils.toArray(".reveal-up");
        if (revealUpElements.length > 0) {
            revealUpElements.forEach(elem => {
                gsap.from(elem, { scrollTrigger: { trigger: elem, start: "top 85%" }, y: 60, opacity: 0, duration: 1, ease: "power2.out" });
            });
        }
    </script>

    <script>
        (function () {
            var mobileMenuBtn = document.getElementById('mobile-menu-btn');
            var mobileMenu = document.getElementById('mobile-menu');
            var mobileLinks = document.querySelectorAll('.mobile-link');
            var hamburgerIcon = document.getElementById('hamburger-icon');
            var closeIcon = document.getElementById('close-icon');

            if (!mobileMenuBtn || !mobileMenu) return;

            function toggleMobileMenu() {
                var isOpen = !mobileMenu.classList.contains('translate-x-full');
                mobileMenu.classList.toggle('translate-x-full');
                document.body.classList.toggle('overflow-hidden');
                mobileMenuBtn.setAttribute('aria-expanded', String(!isOpen));
                if (isOpen) {
                    if (hamburgerIcon) hamburgerIcon.classList.remove('hidden');
                    if (closeIcon) closeIcon.classList.add('hidden');
                } else {
                    if (hamburgerIcon) hamburgerIcon.classList.add('hidden');
                    if (closeIcon) closeIcon.classList.remove('hidden');
                }
            }

            window.aromaToggleMenu = toggleMobileMenu;
            mobileMenuBtn.addEventListener('click', toggleMobileMenu);
            mobileLinks.forEach(function (link) {
                link.addEventListener('click', toggleMobileMenu);
            });
        })();
    </script>

    <!-- Email Deobfuscation Script -->
    <script>
        document.querySelectorAll('.email-link').forEach(el => {
            const email = el.dataset.user + '@' + el.dataset.domain + '.' + el.dataset.tld;
            const link = document.createElement('a');
            link.href = 'mailto:' + email;
            link.textContent = email;
            link.className = 'hover:text-aroma-gold transition-colors';
            const parent = el.parentElement;
            if (parent && parent.classList.contains('flex')) {
                parent.replaceChild(link, el);
            } else {
                el.replaceWith(link);
            }
        });
    </script>

    <!-- Image Protection Script -->
    <script>
        (function() {
            'use strict';

            document.addEventListener('contextmenu', function(e) {
                if (e.target.tagName === 'IMG') { e.preventDefault(); return false; }
            }, false);

            document.addEventListener('dragstart', function(e) {
                if (e.target.tagName === 'IMG') { e.preventDefault(); return false; }
            }, false);

            document.addEventListener('keydown', function(e) {
                if ((e.ctrlKey || e.metaKey) && e.key === 's') { e.preventDefault(); return false; }
            }, false);

            const observer = new MutationObserver(function(mutations) {
                mutations.forEach(function(mutation) {
                    mutation.addedNodes.forEach(function(node) {
                        if (node.tagName === 'IMG') {
                            node.addEventListener('contextmenu', function(e) { e.preventDefault(); return false; });
                            node.addEventListener('dragstart', function(e) { e.preventDefault(); return false; });
                        }
                    });
                });
            });

            observer.observe(document.body, { childList: true, subtree: true });

            const images = document.querySelectorAll('img');
            images.forEach(function(img) {
                img.addEventListener('selectstart', function(e) { e.preventDefault(); return false; });
                img.setAttribute('draggable', 'false');
                img.ondragstart = function() { return false; };
            });

            console.log('%c⚠️ UPOZORENJE / WARNING', 'color: #ff0000; font-size: 24px; font-weight: bold;');
            console.log('%cSve slike na ovoj stranici su zaštićene autorskim pravima Aroma Slastičarne.\nNeovlašteno preuzimanje, kopiranje ili distribucija je zabranjena.\n\nAll images on this page are copyrighted by Aroma Slastičarna.\nUnauthorized download, copying, or distribution is prohibited.', 'color: #c5a059; font-size: 14px; line-height: 1.6;');
        })();
    </script>

    <?php wp_footer(); ?>

</body>

</html>
